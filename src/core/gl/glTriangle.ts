namespace hyperEngine {
    export class GLTriangles
    {
        private _elementSize:number=0;
        private _buffer:WebGLBuffer;
        private _mode:number = gl.TRIANGLES;

        constructor()
        {

        }
    }
}
